# タスクリスト

## 🚨 タスク完全完了の原則

**全タスクが `[x]` になるまで作業を継続すること**

---

## フェーズ1: repository-structure.md の更新

- [x] Bootstrap フェーズ全体構造セクションを先頭に追加する
  - [x] このリポジトリの実際のディレクトリツリーを記載する
  - [x] `.devcontainer/` を「Claude Code 開発環境（Kiro テンプレートには含まれない）」と明記する
  - [x] `kiro-template/` を「Kiro テンプレート成果物フォルダ」として説明する
- [x] 既存「プロジェクト構造（完成形）」セクションを `kiro-template/` 構造に更新する
  - [x] セクションタイトルを「kiro-template/ の構造（展開後の Kiro テンプレート）」に変更する
  - [x] ツリーのパスを実際の `kiro-template/` 配下の構造に合わせる
- [x] 「ディレクトリ詳細」セクションのパスを `kiro-template/` 配下に更新する
  - [x] `.kiro/steering/` → `kiro-template/.kiro/steering/`
  - [x] `.kiro/hooks/` → `kiro-template/.kiro/hooks/`
  - [x] `.kiro/settings/` → `kiro-template/.kiro/settings/`
  - [x] `.steering/` → `kiro-template/.steering/`
  - [x] `docs/` セクションは変更不要（このリポジトリの `docs/` を指す）
- [x] 「テンプレートとしての使い方」セクションのカスタマイズ対象パスを更新する

## フェーズ2: 品質チェック

- [x] 記載されたパスが実際のファイル構成と一致することを確認する
- [x] Bootstrap フェーズとリリースフェーズの構造の違いが明確に説明されているか確認する

## フェーズ3: 振り返り

- [x] 振り返りをこのファイルの下部に記録する

---

## 実装後の振り返り

### 実装完了日
2026-06-03

### 計画と実績の差分

**計画と異なった点**:
- 計画通り実装完了。差分なし。

**新たに必要になったタスク**:
- なし

### 学んだこと

- Bootstrap フェーズのリポジトリは「Claude Code 開発インフラ」と「Kiro テンプレート成果物（kiro-template/）」の2層構造になっており、これを表形式で明示することで読者が混乱しない
- `docs/` はこのリポジトリの永続ドキュメントであり `kiro-template/` の外に置く理由を明記することで、Kiro ユーザーが「自分のプロジェクトでは `docs/` を新規作成する」という流れを理解できる

### 次回への改善提案

- `docs/architecture.md` にも Bootstrap フェーズ構造（2層構造）への言及を追加すると、構造変更の文書的整合性が高まる（スコープ外として今回は対応しなかった）
- `kiro-template/README.md` の「構成」セクションツリーが `kiro-template/` 配下のみを示しているため、このドキュメントとの対応関係は一貫している

### リリース判断

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | リリース不要（ハーネス全体の初回リリースにまとめる） |

**提案**: `docs/repository-structure.md` の更新が完了した。次は Kiro 実環境での受け入れテスト（シナリオ1〜4）の実施により、初回リリースの準備を進める。
