# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: README.md の修正

- [x] クイックスタート Step 3 を `setup-project` 実行フローに書き換える
  - [x] `product.md` / `tech.md` / `structure.md` の手動編集手順を削除
  - [x] `setup-project` のチャット呼び出し方法と説明を追加
  - [x] `setup-project` が docs/ + steering 両方を作成することを明示
- [x] クイックスタート Step 4 の見出しを「最初の機能を実装する」に変更し `add-feature` への接続を明示

## フェーズ2: ONBOARDING.md の修正

- [x] Section 2「ステップ 2」を `setup-project` 実行フローに書き換える
  - [x] 手動編集の説明文を削除
  - [x] tech.md の記入例コードブロック（Windows + Python サンプル）を削除
  - [x] `setup-project` の 3 ステップ（ヒアリング → docs/ 作成 → steering 更新）を箇条書きで追加
  - [x] 手動編集したい場合は付録を参照する旨を追記
- [x] 付録「よくある質問」に手動編集サンプルのセクションを追加する
  - [x] tech.md の Windows + Python 記入例を付録に移動

## フェーズ3: 確認

- [x] README.md を通読し、クイックスタートのフローが `setup-project` → `add-feature` の順になっていることを確認
- [x] ONBOARDING.md を通読し、Section 2 が `setup-project` フローになっており Section 3 への流れが自然であることを確認

## フェーズ4: 振り返りとリリース準備

- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-06-05

### 計画と実績の差分

**計画と異なった点**:
- ONBOARDING.md Section 3 の書き出しが「steering ファイルを記入したら」のままだったため、追加で「`setup-project` が完了したら」に修正した（計画時に見落とし）

**新たに必要になったタスク**:
- ONBOARDING.md Section 3 冒頭の文言修正（setup-project フローとの整合）

### 学んだこと

**プロセス上の改善点**:
- 設計時に変更ファイルを通読し、連鎖的に修正が必要な箇所を事前にリストアップするとよい

### 次回への改善提案
- `add-feature` フックに「docs/ が未作成の場合は setup-project を先に実行してください」というガードを追加することで、ドキュメントではなくフック自体で保護する方法も検討できる

### リリース判断

**前提条件の確認**:
- [x] README.md 通読確認
- [x] ONBOARDING.md 通読確認
- [x] リリースノートに記載すべき変更内容が整理されている

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | PATCH |

**提案**:
ドキュメントのみの修正で機能追加・破壊的変更なし。PR マージ後に `v0.x.x` への PATCH リリースを提案。
