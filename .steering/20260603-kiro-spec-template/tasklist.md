# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
技術的理由（実装方針変更・アーキテクチャ変更・依存関係変更）に該当する場合のみ。
スキップ時は必ず理由を明記: `- [x] ~~タスク名~~（理由: ...）`

---

## フェーズ1: ディレクトリ作成と requirements.md の実装

- [x] `kiro-template/.steering/_template/` ディレクトリを作成する
- [x] `requirements.md` を作成する
  - [x] 概要セクション（プレースホルダー + `<!-- 例: ... -->`）
  - [x] 関連 Issue 記入欄（必須と明示）
  - [x] ユースケースの軸セクション（プレースホルダー + `<!-- 例: ... -->`）
  - [x] 実装対象の機能セクション（複数機能対応）
  - [x] 受け入れ条件セクション（チェックリスト形式）
  - [x] スコープ外セクション
  - [x] 参照ドキュメントセクション（固定リスト）

## フェーズ2: design.md の実装

- [x] `design.md` を作成する
  - [x] 実装アプローチセクション（自由記述欄）
  - [x] 変更対象ファイルセクション（テーブル形式）
  - [x] 技術的判断と根拠セクション
  - [x] 実装の順序セクション
  - [x] `<!-- 例: ... -->` 形式の記入ガイドを各セクションに追加

## フェーズ3: tasklist.md の実装

- [x] `tasklist.md` を作成する
  - [x] タスク完全完了の原則セクション（ルール全文）
  - [x] フェーズ1: 実装（プレースホルダー）
  - [x] フェーズ2: 品質チェック（テスト・リントコマンドはプレースホルダー）
  - [x] フェーズ3: ドキュメント更新
  - [x] 実装後の振り返りセクション（完了日・差分・学び・次回提案）
  - [x] リリース判断セクション

## フェーズ4: 品質チェック

- [x] 全3ファイルに `<!-- 例: ... -->` 形式の記入ガイドが含まれていることを確認する
- [x] `docs/development-guidelines.md` のスペックテンプレート品質基準を確認する
  - [x] `requirements.md`: Issue URL 記入欄・ユーザーストーリー形式・受け入れ条件チェックリスト・スコープ外欄
  - [x] `design.md`: 実装アプローチ欄・変更対象ファイル欄・技術的判断記入欄
  - [x] `tasklist.md`: チェックリスト形式・実装→テスト→ドキュメント更新の順序が明示
- [x] `process.md` の「スペック作成手順」と整合していることを確認する

## フェーズ5: 振り返り

- [x] 振り返りをこのファイルの下部に記録する

---

## 実装後の振り返り

### 実装完了日
2026-06-03

### 計画と実績の差分

**計画と異なった点**:
- 特になし。設計通りに実装できた

**新たに必要になったタスク**:
- 特になし

### 学んだこと

**技術的な学び**:
- Kiro 向けテンプレートは Claude Code 用よりシンプルに保つのが正解。コード向けの詳細セクション（エラーハンドリング・セキュリティ等）は Kiro テンプレートには不要
- `<!-- 例: ... -->` コメントに具体的なコード・コマンド例を含めると、ユーザーが「何を書けばよいか」を即座に理解できる

**プロセス上の改善点**:
- `kiro-template/` フォルダへの配置方針が確立されており、パスに迷わず実装できた

### 次回への改善提案
- `kiro-template/.kiro/hooks/` と `kiro-template/.kiro/settings/mcp.json` の実装を次スペックで行う
- `kiro-template/README.md` の実装も並行して検討する

### リリース判断

**前提条件の確認**:
- [x] 全3ファイルが作成されている
- [x] 品質基準チェックが完了している

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | リリース不要（ハーネス全体の初回リリースにまとめる） |

**提案**:
単独リリースは不要。残りコンポーネント（hooks・mcp.json・README）が揃った段階で初回リリースを行う。
