# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: ワークフロー修正

- [x] `.github/workflows/publish-template.yml` に "Create release on output repo" ステップを追加する
  - [x] `if: github.event_name == 'release'` 条件を設定する
  - [x] 差分なしスキップの検出ロジックを実装する
  - [x] `gh release create` コマンドを実装する（タグ・タイトル・リリースノート）

## フェーズ2: 確認

- [x] ワークフローファイルを通読し、既存ステップとの整合を確認する（id: publish の追加漏れを修正）
- [x] workflow_dispatch 時にリリース作成ステップがスキップされることをロジックで確認する

## フェーズ3: 振り返りとリリース準備

- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-06-05

### 計画と実績の差分

**計画と異なった点**:
- 設計では `git log` のコミットメッセージで差分なしを検出する方針だったが、`GITHUB_OUTPUT` への `published=true` 書き出しで判定する方式に変更（より確実）

**新たに必要になったタスク**:
- "Commit and push" ステップへの `id: publish` 追加（通読確認で発見）

### 学んだこと

**技術的な学び**:
- GitHub Actions でステップ間の出力を受け渡すには `id:` の設定と `$GITHUB_OUTPUT` への書き出しが必要

### 次回への改善提案
- 将来的にはリリースノートを本リポジトリから取得して転記することも検討できる

### リリース判断

**前提条件の確認**:
- [x] ワークフロー通読確認

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | PATCH |

**提案**:
ワークフローのみの変更。PR マージ後に `v0.3.3` への PATCH リリースを提案。
